spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://127.0.0.1:3306/mydb?useUnicode=true&characterEncoding=utf8&useSSL=false

logging:
  level:
    org:
      springframework:
        jdbc:
          core=TRACE:



## 配置 mybatis xml 路径
mybatis:
  mapper-locations: classpath:mapper/*.xml

